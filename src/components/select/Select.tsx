import styled from "styled-components";

import React from "react";
import SelectWrapper from "./SelectWrapper";

export const Select = () => {
  return (
    <SelectWrapper>
      <Label></Label>
    </SelectWrapper>
  );
};
